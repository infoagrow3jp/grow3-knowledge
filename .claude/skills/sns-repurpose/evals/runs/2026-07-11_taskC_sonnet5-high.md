# eval実行記録：タスクC（SNS生成／sns-repurpose）

## 実行環境

- 日付：2026-07-11
- 環境：Cursor Agent
- モデル：Sonnet 5 High
- ラン数：3
- スキルのバージョン：本記録が起点となる改定前（このrun後にSKILL.md／evals.jsonを改定）

## 結果

- 8条件中7条件×3ラン＝21/21 PASS
  （断面分離・匿名化・CTA温度・frontmatter・未完結性・1論点、いずれも安定）
- 残る1条件（親投稿＝3行以内のフック）は3/3ランで不合格（詳細は下記「検出した仕様の穴」）

## キャリブレーション決定（小林さん裁定）

1. 親投稿＝手動改行で3行以内かつ120字以内とする（端末上の自動折り返しは行数に含めない）
2. 読者への問いかけ型の仮想台詞は許容する。ただし実在の発言・相談実績として表現するのは禁止する

## 検出した仕様の穴

親投稿の行数が3/3ランで超過（4〜5行）。「3行以内のフック」という記述だけでは
強制力が不足していると判定。手動改行・字数の基準が明文化されていなかったため、
モデルが自動折り返し込みで行数を数えたり、文を増やしたりしていた。

→ スキル強化（SKILL.md）と回帰条件追加（evals.json）を行い、再実行する。

## 運用の穴

テスト生成物（本eval用の架空記事から作った投稿ファイル）が、判定用の一時領域ではなく
本番フォルダ `marketing/sns/` に保存されていた。実運用のコンテンツ管理と混在するリスクが
あるため、evals-README.mdの運用ルールを強化して対処する。

## 次のアクション

- `SKILL.md`：親投稿の定義（3行・120字、手動改行基準）と仮想台詞ルールを明文化
- `evals.json`：親投稿の行数・字数条件を必須条件として追加（回帰確認用）
- `evals-README.md`：生成系タスクの生成物を本番領域に保存させない運用ルールを追記

## 再実行結果（スキル強化後・正式確定）

- 再実行環境：スキル強化後（コミット 7454599）、完全独立3ラン
- 結果：9条件×3ラン＝27/27 PASS
- 条件9（親投稿＝3行以内・120字以内）は機械計測で確認：親投稿9本すべて3行以内・
  27〜56字（120字上限に対して大幅余裕）。うち2行の親も3本あり、120字を
  目標値ではなく上限として扱う解釈が正しく機能していることを確認した。

## 確定

タスクCを正式確定する。
