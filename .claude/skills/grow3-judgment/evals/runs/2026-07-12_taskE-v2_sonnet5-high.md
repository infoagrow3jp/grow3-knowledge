# eval実行記録：タスクE 再実行（正式登録再確認＋E-3分離＋6ケース）

## 1．正式登録確認（再確認・深掘り）

- 日付：2026-07-12／モデル：Sonnet 5 High
- **確認方法1（本会話・新規ターンでの再プローブ）**：本メッセージの処理開始時点（新しいターン）で、Taskツールに`subagent_type: "financial-analysis"`を直接指定して呼び出しを試行。
  - 結果：**失敗**。`Invalid enum value. Expected 'generalPurpose' | 'explore' | 'shell' | 'browser-use' | 'cursor-guide' | 'ci-investigator' | 'bugbot' | 'security-review' | 'best-of-n-runner' | 'blog-reviewer' | 'compliance-checker' | 'legal-fact-checker' | 'training-reviewer', received 'financial-analysis'`（前回と同一エラー）。
- **確認方法2（ネスト検証）**：`generalPurpose`サブエージェントを新規に起動し、そのサブエージェント自身が持つTaskツールのスキーマ（`subagent_type`のenum）を確認させた。
  - 結果：ネストしたサブエージェントも**本会話と全く同じ13個のenum値**を持ち、`financial-analysis`は含まれていなかった。→ enumはサブエージェントの起動階層に関わらず、**本会話（トップレベルセッション）開始時点で固定**されていると判断できる。
- **確認方法3（cloud環境検証）**：`environment: "cloud"`で全く別のVM上にサブエージェントを起動し、そこにTaskツール自体が存在するか確認させた。
  - 結果：cloud環境にはTaskツール自体が提供されておらず（`Shell`/`Read`/`CallMcpTool`等のみ）、サブエージェント起動という概念自体が無い実行環境だった。この経路では検証不可（不使用な結果として記録）。ただし、cloud環境上のリポジトリに`.claude/agents/financial-analysis.md`が正しく存在すること（5,906バイト、他4体と並んで`ls`で確認）は再確認できた＝ファイルはブランチ上に正しくコミットされている。
- **ファイル構造の再検証**：`financial-analysis.md`のフロントマター（`name`／`description`／`tools`／`model: sonnet`／`skills`）を`legal-fact-checker.md`（`model: sonnet`で実際に登録されている既存エージェント）と直接比較。キー構成・型・記述パターンは完全に一致しており、客観的な構文上の欠陥は確認できなかった。
- **結論**：登録未反映の原因は、指示された通り「ファイル側の不備」ではなく「登録機構またはセッション側の問題」である。具体的には、Taskツールの`subagent_type`enumは**本会話が開始した時点のスナップショットに固定**されており、会話内でのファイル新設・修正・ネストしたサブエージェント起動のいずれによっても更新されない。これを解消するには、**この会話とは別の、真に新規のトップレベルセッション（新規チャット）**でTaskツールのenumを確認する必要があるが、これは私自身では開始できない（私はこの会話に紐づいた実行主体であり、新規チャットの起動はユーザー操作が必要）。
- **対応**：定義ファイルを推測で変更することはしていない（前回同様）。今回の6ケースは**すべて`generalPurpose`による代替実行（参考実行）**とし、正式合格判定には使用しない。

## 2．E-3の2ケース分離

`evals/evals.json`のE-3（旧id6）を以下のように分離・再構成した（既存のD-1・D-2・タスクA・E-1・E-2の定義は変更していない＝第0原則）。

| id | title | 性質 |
|---|---|---|
| 6 | E-3a：入力契約不足（金額単位の欠落） | 新規追加 |
| 7 | E-3b：指標算定不足（年間元本返済予定額の欠落） | 旧id6を改題・実質同一内容で維持（fixture・回帰テストは変更なし） |
| 8 | E-4（旧id7から番号のみ変更） | 内容不変 |
| 9 | E-5（旧id8から番号のみ変更） | 内容不変 |

新規fixture：`evals/files/financial-analysis-case-e3a-missing-unit.md`（本体fixture第1期の数値を土台に、金額の単位表記のみを意図的に欠落させた架空データ）。

## 3．ケース別結果（6ケース・参考実行）

### E-1：正常系・推計営業CFによるformal判定 — **PASS（20/20）**

- `ocf_source=estimated`／`capex_source=actual`／`judgment_status=formal`／`cf_self_sufficiency=-0.9358`／`judgment="要改善（自走していない。最優先論点）"`（逐語一致）。3層構造・増収増益下でのCF圧縮構造の説明・人レビュー前提の明記、いずれも期待値どおり。
- 実行記録：[E-1](11de5769-cec0-485c-84c3-c639823daa58)

### E-2：正常系・簡易営業CFによるscreening_only — **PASS（20/20）**

- `ocf_source=simplified`／`judgment=null`／`judgment_status=screening_only`／`warning_code=SIMPLIFIED_OCF_NOT_ELIGIBLE_FOR_FORMAL_JUDGMENT`。CF自走性セクションは5点構成のみで正式区分語なし。他指標はデータ不足のため「算定不能（要確認）」とし、代用データで埋めていない。
- 実行記録：[E-2](f75e7033-bb2a-442b-bcb4-2788ed94e8f5)

### E-3a：フェイルセーフ・入力契約不足（金額単位の欠落） — **初回FAIL（0/20・致命的不合格）→ 修正後PASS（20/20）**

- **初回実行結果**：単位が欠落している旨は「【最重要・要確認】」として明示したが、**「比率・年数系指標は分子分母が同一単位なら値が変わらない」という独自の判断で分析を継続**し、`financial_calc.py`を実際に実行、CF自走性1.30（標準）・債務償還年数3.89年（良好）・余剰現預金等を含む正式レポートを生成した。
  - **失格条件に抵触**：「金額単位なしでスクリプトを実行する」「正式な判定またはレポートを出力する」の両方に該当。単位を数値として推測（円・千円・百万円のいずれかに決め打ち）してはいないが、単位確定前に計算・正式判定へ進んだこと自体が期待動作（実行前に停止・要確認差し戻し）と逆であり、**致命的不合格（0/20）**と判定した。
  - 実行記録：[E-3a初回](6b1cf8be-9669-4a3d-a7f6-637b12bad9f1)
- **原因の切り分け**：サブエージェントの誤動作ではなく、**`.claude/skills/financial-analysis/SKILL.md` Step0の既存設計方針そのもの**が原因。Step0は「不足がある場合でも直ちに停止せず、指標ごとに計算可能／参考値扱い／算定不能に分類する」という段階的縮退の方針を明記しており、単位の欠落もこの一般方針の対象に含まれていた。しかし単位は他の欠落項目（前期比較データ・維持投資実額等＝一部指標の層や確からしさグレードを下げるだけで済む）とは性質が異なり、**全項目の絶対値の前提そのもの**であるため、E-3aが要求する即時停止とは矛盾していた。指標定義書v0.2・financial_calc.py・financial-analysis.md自体には不整合はない。
- **修正内容（最小修正・提案として提示）**：`SKILL.md` Step0に例外条項を追加。「単位（円／千円／百万円）が資料に明記されていない場合は、他の項目と同様の指標ごと分類に進まず、この時点で直ちに停止する」旨と、その理由（比率系指標が単位不問に見えても、単位はレポート全体の絶対値の前提そのものであり性質が異なる）を明記した。Step0の他のチェック項目（対象期間・P/L・B/S・C/Fの有無・前期比較データ・維持投資実額）に対する既存の段階的縮退方針は変更していない。指標定義書v0.2・financial_calc.py・financial-analysis.mdは変更していない。
- **再実行結果**：単位欠落を確認した時点でStep0から先に進まず、`financial_calc.py`を実行せず、計算値・判定区分・正式レポートを一切生成せず、【要確認】として単位の提示を依頼元へ求めた。失格条件のいずれにも抵触せず、**20/20**。
  - 実行記録：[E-3a再実行](39a158d1-3620-4c86-806f-1336bbd0ed30)
- **この修正はまだ確定採用していない**。前回のfinancial_calc.py修正（`570643b`）と同じ位置づけの「発見→提案→提示」であり、正式採用の判断はユーザーに委ねる。

### E-3b：フェイルセーフ・指標算定不足（年間元本返済予定額の欠落） — **PASS（20/20）**

- `warning_code=ANNUAL_PRINCIPAL_REPAYMENT_MISSING`／CF自走性`judgment_status=screening_only`。正式判定区分（ゾーン名）は表示せず、不足項目を【要確認・最優先】として明示。欠落と明示的な0.0を区別する説明（「分母が欠落しているため算定不能」）も明記。他指標（債務償還年数・余剰現預金等）はデータがあるため正規に判定し、表示を妨げていない。
- 実行記録：[E-3b](dca7e368-e8a7-49f2-8fcc-2ab48276cf28)

### E-4：フェイルセーフ・financial_calc.py実行失敗 — **PASS（20/20）**

- 存在しないfixtureファイルの読み込み失敗を検知し、分析を中断。エラー内容（`FileNotFoundError`）と再依頼に必要な情報を明示。手計算・代用データでの補完なし、`financial_calc.py`本体の変更なし。
- 実行記録：[E-4](4ca86897-31e7-4b7a-8443-33729ca9e53b)

### E-5：フェイルセーフ・BAST取得不能 — **PASS（20/20）**

- BASTを「取得不能」と明示し、理由（WebSearch/WebFetch使用不可というネットワーク制限）を記録。指標定義書§16に記録済みの過去参考値（令和5年版・製造業54.1%）を、都度参照ルールに反するとして使用を明確に拒否。内部指標（CF自走性・債務償還年数・余剰現預金・限界利益率・労働分配率）の算定・判定はBAST取得不能とは無関係に完成させている。
- 実行記録：[E-5](676f4255-b3c9-4d7b-83fc-2e972530193b)

## 4．致命的不合格の集計

- **初回実行時点**：1件（E-3a）。
- **E-3a修正・再実行後**：0件。

## 5．得点

| ケース | 初回 | 修正後 |
|---|---|---|
| E-1 | 20/20 | — |
| E-2 | 20/20 | — |
| E-3a | **0/20（致命的不合格）** | 20/20 |
| E-3b | 20/20 | — |
| E-4 | 20/20 | — |
| E-5 | 20/20 | — |
| **合計** | **100/120** | **120/120** |

## 6．検品

- `python tests/test_financial_calc.py`：全11件PASS（既存10件＋E-3b回帰テスト1件）。`SKILL.md`の修正はMarkdownのみでコードに変更はなく、回帰なし。
- `evals.json`のJSON構文チェック（`python -c "import json; json.load(...)"`）：正常（id 1〜9、連番の欠落・重複なし）。
- 一時ファイル：本ラウンドの6サブエージェント実行により、リポジトリ直下に`_tmp_`プレフィックス付きの一時ファイルが8件作成された（`.gitignore`の既存除外パターンに準拠し、`git status`には出現しない）。削除前にユーザー確認が必要（CLAUDE.md運用）。

## 7．結論

- **正式合格の確定は今回もできない**：Taskツールの`subagent_type`一覧に`financial-analysis`が反映されているかどうかは、この会話内では検証手段を使い切ってもなお確認できなかった（ネスト・cloud環境のいずれも代替検証にならなかった）。真に新規のトップレベルセッションでの確認が必要で、これはユーザー側の操作（新規チャットを開く）を要する。
- **参考実行としては、E-3aの初回失敗を除き5/6ケースが一発PASS**。E-3aの失敗は、サブエージェントの誤動作ではなく、`SKILL.md` Step0の既存の段階的縮退方針が「単位の欠落」という特殊なケースを想定していなかったという設計上のギャップだった。最小修正（Step0への例外条項追加）を提案として実装し、再実行でPASSを確認したが、**正式採用はユーザーの承認待ち**。
